---
identifier: "CMP-000003"
record_type: component
subject: "ROL:DKS deck-support channel component — SEC:OCU 32×25×1.5 mm section (GS-000002), straight (no end flare); bound length 1170 mm"
status: RESERVED
born_snapshot: SNAP-1.0.0
governed_by: Material Handling Taxonomy Standard
authorizing_decision: "Founding component registration under DECISION-EF-SEED-001 (docs/STEWARD_SEEDING_DECISION_EF.md; bootstrap clause GOVERNANCE §2; Registry Architecture §9.2–§9.3); recorded in docs/ENGINEERING_LOG.md (2026-07-12, Category E/F founding proof records); ConfigurationID derived under Configuration Canonicalization Rules v1 against template ROL-DKS.v1; serial issued in First 100 Records Plan §1/§4 Category-E enumeration order (deck-support channel third); activation gate per FIRST_100_RECORDS_PLAN §9"
derived_id: "CF1-265CDD59847D"
---

# component record — `CMP-000003` (ROL:DKS deck-support channel, SEC:OCU, bound L = 1170 mm)

*Founding registry record. Format: [FOUNDING_RECORD_TEMPLATE](../../docs/FOUNDING_RECORD_TEMPLATE.md) (component variant). Category E of the [First 100 Records Plan](../../docs/FIRST_100_RECORDS_PLAN.md).*

## 1. Identity
- **Identifier:** `CMP-000003` — a registered ComponentID (MH Taxonomy Standard §4.9). Issued in First 100 Records Plan §1/§4 Category-E enumeration order (deck-support channel is the third component). Not the illustrative `CMP-000612` of Standard Part 6 (a placeholder, reconciled per [First 100 §4](../../docs/FIRST_100_RECORDS_PLAN.md)).
- **Record type:** Component definition (§4.9); a monolithic roll-formed U-channel acting as a single linear support member.
- **Subject:** the U-channel **deck-support member** in role [`ROL:DKS`](../dictionary/ROL-DKS.md), section geometry [`GS-000002`](../gsid/GS-000002.md) (`SEC:OCU` 32×25×1.5 mm). **Prismatic**; its length and end treatment are **configuration** (rule G-3). The definition is parametric in length (bound per deck depth, §6.3); this record binds **L = 1170 mm** with straight (unflared) ends.

## 2. Meaning
A ComponentID identifies one registered component definition (§4.9). This channel is prismatic — its cross-section is registered geometry `GS-000002`; its length and end condition are configuration.

**Bound template:** [`ROL-DKS.v1`](../../templates/ROL-DKS.v1.md) — the version-1 configuration template for `ROL:DKS` (Taxonomy Standard §4.7).

**Identity-bearing configuration (values from Standard Part 6):**

| CFG group | Field | Value | Unit / class | Source |
|---|---|---|---|---|
| [`CFG:DIM`](../dictionary/CFG-DIM.md) | `length_mm` | 1170 | mm (L) | Part 6.3 "parametric length, bound per deck depth" + Part 6.1 deck depth 1170 mm |
| [`CFG:END`](../dictionary/CFG-END.md) | `end_flare` | `FALSE` | boolean | Part 6.1 "straight legs"; Part 6.4 (ends flared only is the alternative) |

**Length derivation (transparent):** the channel definition is parametric in length, "bound per deck depth" (§6.3). The Part 6 deck is "1170 mm deep" (§6.1) and the channels span front-to-back (support spacing `[290, 580, 290]` sums to the 1160 mm width, so the members run the 1170 mm depth). Bound `length_mm = 1170`.

**End-flare = FALSE** (the base deck uses "straight legs", §6.1). Because `end_flare = FALSE`, the conditional fields `flare_length_mm` and `flare_angle_deg` are **omitted entirely** from the hash (Configuration Canonicalization Rules v1 §9.3 omission rule). This channel binds **no closed-enum token** (its only categorical field is the boolean `end_flare`, serialized as `TRUE`/`FALSE` per rules §7), so it draws no `enum_tokens.csv` entry. `section_ref` → `GS-000002` is a component-definition reference, not a hashed field (rules §3).

**Derived ConfigurationID:** `CF1-265CDD59847D` (see §6).

## 3. Lifecycle
- **Current status:** `RESERVED` — allocated by DECISION-EF-SEED-001, pending activation at the `SNAP-1.0.0` cut (Registry Architecture §5/§8); object-record `RESERVED → ACTIVE` is the cohort's one `[Proposed]` dependency (First 100 §6 item 6), flagged pending.
- **Event history:**
  - *Reserved* — 2026-07-12, founding component registration under the bootstrap clause; authorizing decision [`DECISION-EF-SEED-001`](../../docs/STEWARD_SEEDING_DECISION_EF.md), recorded in [ENGINEERING_LOG](../../docs/ENGINEERING_LOG.md). Basis: Standard Part 6 worked example.
- **Next transition:** `RESERVED → ACTIVE` at the `SNAP-1.0.0` release (First 100 §9), pending the object-record lifecycle adoption (§6 item 6) and readiness checklist (§10).

## 4. Provenance
- **Decided by:** the founding maintainer acting as Registry Operator/TC under the bootstrap clause (GOVERNANCE §2; Registry Architecture §9.2–§9.3), authorized by DECISION-EF-SEED-001.
- **Source:** project-authored — the wire-deck worked example of Taxonomy Standard Part 6 (§6.1, §6.3–6.4). Zero external permission, no third-party data (First 100 §5 E+F). No manufacturer, catalog, or license.
- **License:** repository `LICENSE` not yet finalized; no license granted.

## 5. Relationships
- **Role / section:** role [`ROL:DKS`](../dictionary/ROL-DKS.md); section geometry [`GS-000002`](../gsid/GS-000002.md) (`SEC:OCU` 32×25×1.5 mm, `derived_id` `CG1-710171128D0C`).
- **Bound template:** [`ROL-DKS.v1`](../../templates/ROL-DKS.v1.md). **Enum vocabulary:** none used (only the boolean `end_flare`). **Derivation rules:** [Configuration Canonicalization Rules v1](../../standards/canonicalization/configuration/v1/CONFIGURATION_CANONICALIZATION_RULES_V1.md) + [schema](../../standards/canonicalization/configuration/v1/schemas/ROL-DKS.v1.md).
- **Consumed by:** the wire-deck assembly [`ASP-000001`](../assembly/ASP-000001.md) — bill of roles `ROL:DKS → CMP-000003 ×3`; also the assembly's hashed field `CFG:SUP.support_component = CMP-000003`.
- **Predecessor / successor:** none (first issuance). A continuously-flared-leg variant would be a **different** `SEC:OCU` GSID (D-2); ends-flared-only would be a new ConfigurationID of this same channel (G-3).
- **Crosswalks / aliases:** none at issuance.

## 6. Reproducibility
- **Snapshot of birth:** `SNAP-1.0.0` (pending cut). **Current view rendered from:** pre-release (R0).
**Normalized byte string** (UTF-8, no trailing newline; rules §9, template field order; conditional flare fields omitted per §9.3) — 64 bytes, exactly the bytes hashed (copy the fenced line flush-left, no leading spaces):

```
CANON-CFG-V1|ROL-DKS.v1|DIM.length_mm=1170.0;END.end_flare=FALSE
```
- **SHA-256:** `265cdd59847dc99dcfabc18bb87b07fd1efe6a427065f488d0d1068d5d3df382`
- **Recompute (rules §10):** `printf '%s' '<byte string above>' | sha256sum` → first 12 hex, upper-case, prefix `CF1-` → **`CF1-265CDD59847D`** = `derived_id`. Reproducible from published files alone (P9). (This is a **production** value: same grammar as the rules' non-cohort reference Vector A, but `length_mm=1170.0` not `2000.0`, so a distinct hash.)

## 7. Disclaimer
Identity and traceability only. **Identity is not certification; classification is not performance.** This record makes no manufacturer, SKU, brand, load-capacity, structural-performance, safety, test-result, or compliance representation (Registry Architecture invariant 8; refusal rule 3; §11 boundary).
