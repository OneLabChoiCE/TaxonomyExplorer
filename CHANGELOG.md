# Changelog

All notable dictionary and standard changes, summarized per snapshot release ([GOVERNANCE.md](GOVERNANCE.md) §5). Detailed rationale lives in [proposals/](proposals/); rejections in [REJECTIONS.md](REJECTIONS.md).

## [Unreleased]

- **Steward-seeding decision recorded** (`docs/STEWARD_SEEDING_DECISION_EF.md`, ID `DECISION-EF-SEED-001`) — the final Category E/F prerequisite (First 100 Records Plan §6 item 5, §8 step 3). A narrow founding-cohort authorization for **exactly the four planned E/F proof records** — the rack-upright, mesh-panel, and deck-support-channel components and the wire-deck assembly — bound to their published Category-C templates, referencing the registered GSIDs (`GS-000001`/`GS-000002`/`GS-000004`), each `RESERVED`, each citing `DECISION-EF-SEED-001`, each with a `CF1-` ConfigurationID recomputable from published files. Explicitly **out of scope**: general/public submissions, third-party/manufacturer/catalog data, SKUs/brand identity, certification, and any capacity/performance/safety/compliance claim (all reaffirmed as refused). Reconciles the founding cohort with the Submission Model's rule that `CMP-`/`ASP-` product-instance intake opens only at stage S3 (§7): the steward registering the project's **own** worked examples under the bootstrap clause is not third-party S3 intake, so the decision records a bounded exception **without opening S3, creating any submission pathway, or weakening any Submission-Model rule**; it sunsets once the four records are authored, and future intake follows the normal S3/governance path. Creates no architecture, record type, or governance rule. **No component/assembly record authored; no production `ConfigurationID` computed; no activation; no snapshot cut.** `registry/component/README.md` and `registry/assembly/README.md` updated to show all E/F prerequisites satisfied — **Category E/F is now authorable** (remaining gates govern the `SNAP-1.0.0` cut, not the authoring). No change to standards' normative text, dictionaries, template field schemas, GSID records, the Submission Model, governance, or Explorer (2026-07-12).

- **`dictionaries/enum_tokens.csv` published** — the governed `UPPER_SNAKE` enum-token vocabulary for the closed-domain `CFG:` fields, the second of Category E's two non-governance prerequisites (after the CF-hash procedure). Seeded from the CFG-group value lists in Taxonomy Standard §3.6 (repository-structure migration step 3) plus the four Category-C templates and worked examples; **24 tokens** across exactly the seven closed-enum domains the four founding templates use — `EDG.edge_style` (FLUSH/WATERFALL/REVERSE_WATERFALL/BOXED), `FIN.system` (POWDER/PAINT/PRE_GALV/HOT_DIP_GALV/ZINC_PLATE), `JNT.method` (RESISTANCE_WELD/FUSION_WELD/CLINCH/BOLT/RIVET/INTERLOCK), `MPT.pattern` (SQUARE/RECT), `SPD.span` (FRONT_TO_BACK/SIDE_TO_SIDE), `SUP.orientation` (LEGS_DOWN/LEGS_UP), `WIR.wire_condition` (PRE_GALV/POST_GALV/PLAIN) — the full declared domain of each field so the canonicalization "governed superset" is complete, bounded strictly to the four templates (no domains for unused CFG groups, no speculative tokens). **No token invented:** 21 tokens are published verbatim in §3.6; `WIR` PRE_GALV/POST_GALV trace to §3.6 "pre/post-galvanized"; the lone `WIR.wire_condition = PLAIN` traces only to the Category-C templates (§3.6 lists neither) and its `source` column says so — included so no template-permitted value is left ungoverned, with its weaker provenance disclosed. Open string/spec fields (`MAT.spec`/`grade`, `mesh_grade`, `HOL.pattern_id`/`faces`, `END.top`/`bottom`, `JNT.weld_schedule_class`) and the boolean `END.end_flare` deliberately get **no** token list (the canonicalization rules serialize those verbatim; a closed vocabulary would be fabrication). Columns `cfg_group,field,token,status,definition,source,since_snapshot` mirror `cfg_groups.csv` (`ACTIVE`/`SNAP-1.0.0`); adds `dictionaries/README.md` documenting the enum-token-authority vs field-schema-authority boundary. Configuration Canonicalization Rules v1 (§8.1/§11/§12) and the four templates + both registry READMEs updated to cite it as published (their prior "not yet published" clauses were stale). **No component/assembly record authored; no production `ConfigurationID` computed; steward-seeding decision not recorded (out of scope).** Category E now has one remaining blocker: the steward-seeding decision (First 100 Records Plan §6 item 5). No change to other `dictionaries/*.csv`, standards' normative text, template field schemas, GSID records, governance, or Explorer (2026-07-12).

- **Configuration Canonicalization Rules v1 published** (`standards/canonicalization/configuration/v1/`) — the Category E `ConfigurationID`-hashing prerequisite every published Category-C template already flagged as unpublished, identified as the primary blocker by a same-session readiness assessment. Adds the normative byte-serialization/rounding/hash procedure that turns a Configuration Template's bound identity-bearing field record into a `CF1-…` ConfigurationID (Taxonomy Standard §4.7–4.8), a sibling package to geometry Canonicalization Rules v1 (`CG1-…`), additive only. Covers every field kind the four founding templates use: three numeric precision classes (length 0.1mm/1dp, wire 0.05mm/2dp, angle 0.1°/1dp — the first two chosen for cross-package consistency with geometry's own classes, the angle class new), exact integers, `TRUE`/`FALSE` booleans, verbatim closed-enum tokens, verbatim open-string/spec fields (delimiter-excluding), ordered lists, and registered-identifier references (serialized as the identifier string only, never the referenced object's fields). Defines the keyed `group.field=value` serialization grammar (ascending CFG-group order, then each template's own field order) with an explicit conditional-field omission rule — the one structural departure from geometry v1's fixed-position vector, disclosed as necessitated by configuration's heterogeneous field kinds. Ships four per-template canonicalization schemas (`schemas/ASM-WDK.v1.md`, `ROL-COL.v1.md`, `ROL-DKS.v1.md`, `ROL-MSH.v1.md`) and three `sha256sum`-verified reference test vectors plus one unhashed illustrative fragment — all deliberately synthetic (placeholder `GS-999999`/`CMP-999999` identifiers, explicitly non-registered) so none can be mistaken for production data: `CF1-4861AB2ADA2E`, `CF1-CB4FCC0C895E`, `CF1-FD320D3A0C4B`. **No component or assembly record authored; no production `ConfigurationID` computed.** Two independent Category E prerequisites remain unaffected: `dictionaries/enum_tokens.csv` (not yet published) and the steward-seeding decision (First 100 Records Plan §6 item 5, not yet recorded) — `registry/component/README.md` and `registry/assembly/README.md` corrected to list all now-accurate prerequisites (both previously omitted the ConfigurationID-hashing prerequisite entirely). The four Category-C templates' "ConfigurationID hashing" paragraphs updated to cite the newly-published package (no field schema, IB/INF split, or field order changed). No change to dictionaries, the GSID records, geometry Canonicalization Rules v1, standards, governance, or Explorer; `FIRST_100_RECORDS_PLAN.md` deliberately left unedited (2026-07-12).

- **Category C configuration templates seeded** (`templates/`) — the 4 founding configuration template records (First 100 Records Plan §1–2). Creates the `templates/` directory and seeds `ASM-WDK.v1` (wire deck, Standard Part 6.5–6.6), `ROL-COL.v1` (upright/column, Part 7.3/7.6), `ROL-DKS.v1` (deck-support channel, Part 6.3–6.4), and `ROL-MSH.v1` (mesh panel, Part 6.3/6.6 — the fourth template §2 added to the Bootstrap sketch). Each is a versioned, TC-registered **field schema** (identifier form `ASM-<code>.v<n>`/`ROL-<code>.v<n>`; Registry Architecture §2 Configuration Type, §9.1) whose Meaning block restates the standard's **identity-bearing (IB) / informative (INF)** field split verbatim, marks canonical units (mm; `UPPER_SNAKE` enums) and a deterministic field order, links every field's CFG dictionary code, and cites its source Part; `CFG:FIN.color` is the one INF field, and channel thickness / load capacity are excluded per Part 6.5. Section/stock refs use the **real registered GSID serials** (`GS-000001`/`GS-000002`/`GS-000004`), not the standard's illustrative placeholders (reconciled per §4). **Two honesty boundaries:** (1) **no `ConfigurationID` is computed** — the byte-serialization + rounding rules for the `CF1-…` hash are a separate, not-yet-published prerequisite (exactly as Canonicalization Rules v1 was for `CG1-…`), so Category E/F hashing stays gated on it and `dictionaries/enum_tokens.csv`; (2) **no fabricated fields** — `ROL-DKS.v1` carries only the fields Part 6.3–6.4 enumerates; anything the standard omits enters as a new template version (`.v2`), never an edit (RA §3.3). Authored as Markdown (JSON encoding deferred, the canonicalization-schema precedent); `record_type: configuration_template` instantiates the existing RA §2 Configuration Type — no new record type. Status `RESERVED` on the single `RESERVED → ACTIVE` trajectory, pending the template-adoption decision (§6 item 2) and the object-record lifecycle adoption (§6 item 6). **No template activated; no snapshot cut.** **Category C complete (4/4).** Founding cohort now A(6)+B(86)+C(4)+D(8) = 104 registered records seeded; remaining E+F (3 CMP + 1 ASP). No change to dictionaries, registry/dictionary records, the GSID records, the canonicalization package, standards, governance, Explorer, or Bootstrap Plan; only `registry/README.md` updated alongside (one stale clause) (2026-07-12).

- **Category D GSID proof cohort registered** (`registry/gsid/`) — the 8 founding geometry records (First 100 Records Plan §4). Seeds `GS-000001`…`GS-000008`, one per flagship worked-example section: `SEC:OCL` 76.2×41.3×12.7×1.9, `SEC:OCU` 32×25×1.5, `SEC:OCU` 40×40×1.5, `SEC:RBR` ⌀4.0, `SEC:PLT` 152×6.4, `SEC:ANG` 38×38×2.7, `SEC:FBR` 25×3, `SEC:SHS` 76×76×3.2. Each carries its derived `CG1-` companion, **computed deterministically under Canonicalization Rules v1** (SHA-256 of the published normalized byte string → 12-hex upper-case → `CG1-`; not hand-written) and **independently reproducible** from the byte string embedded in each record (P9): `CG1-8EF31CC7EA1F`, `CG1-710171128D0C`, `CG1-7076AF79DD56`, `CG1-8728D6FACFC3`, `CG1-F6C764B089B4`, `CG1-686CDC3CB280`, `CG1-648776464448`, `CG1-8B0B6C97B279` (all distinct; the two OCU records differ by D-2; PLT vs FBR differ by shape code as a hashed field). Serials are opaque and issued in First 100 Records Plan §4 enumeration order — real `GS-000001…`, **not** the illustrative `GS-004217`-style placeholders (reconciled per §4). Each record identifies **geometry only** — no product/component/assembly/manufacturer/SKU/capacity/performance/test/certification field (GSID Standard P2; AP-3/AP-4) — links its `SEC:` dictionary code and canonicalization v1 schema, and is `RESERVED` on the single `RESERVED → ACTIVE` trajectory; the RA §5 object-record lifecycle is flagged as the cohort's one `[Proposed]` dependency. **No record activated; no snapshot cut.** **Category D complete (8/8).** Founding cohort now A(6)+B(86)+D(8) = 100 registered records seeded; remaining: C (4 templates) and E+F (3 CMP + 1 ASP). No change to dictionaries, registry/dictionary records, the canonicalization package, standards, governance, Explorer, or Bootstrap Plan; only `registry/gsid/README.md` populated alongside (2026-07-12).

- **Canonicalization Rules v1 published** (`standards/canonicalization/v1/`) — the Category D GSID prerequisite. Adds the normative, versioned procedure that derives a reproducible `CG1-<hash12>` CanonicalGeometryID from a resolved 2D section's canonical parameter vector: `CANONICALIZATION_RULES_V1.md` (scope, canonical unit mm, parameter ordering, two-class numeric normalization [envelope 0.1 mm, thickness 0.05 mm], orientation N/A by construction, conservative equivalent-transform table, **byte-exact serialization** [UTF-8, `|`/`;` separators, case-sensitive, no whitespace, fixed decimals, no trailing newline], the **SHA-256 → 12-hex → `CG1-`** hash procedure with a verified non-cohort reference test vector `CG1-8424749059A5`, explicit exclusions, and a Category D readiness statement), a package `README.md`, and seven per-shape schemas under `schemas/` — `OCL`/`OCU`/`RBR`/`PLT`/`ANG`/`FBR`/`SHS` (the seven Category D proof shapes; `OCU` serves both OCU records). v1 canonicalizes an **intrinsic scalar parameter vector, not coordinate geometry**, so it carries no CAD/orientation ambiguity; the one disclosed simplification is a sharp-corner idealization (radii/taper excluded, §10). No manufacturer/SKU/product/component/capacity/performance/test/certification field appears anywhere. This **clears Category D's canonicalization blocker** (every cohort CGID now recomputable from published files alone, P9); production CGIDs are deliberately **not** computed here — that is the Category D task. Realizes the rules referenced by GSID Standard §4.3–§4.4 without modifying it; no new architecture, governance, or doctrine; no GSID redefinition; no snapshot cut; only `registry/gsid/README.md` prerequisite note updated alongside (2026-07-12).

- Dictionary founding records, batch 8 (`CFG:` namespace completion — **Category B ratified 86/86**): seeded the 11 remaining `CFG:` configuration-group codes under `registry/dictionary/` — `CFG:MAT`/`CFG:EDG`/`CFG:MPT`/`CFG:WIR`/`CFG:SPD`/`CFG:SUP`/`CFG:JNT`/`CFG:FIN`/`CFG:PRF`/`CFG:HOL`/`CFG:END` — each a verbatim, sourced restatement of its `dictionaries/cfg_groups.csv` row (the configuration-group CSV carries `example_fields`, no free-text definition and no `successor` column). All are activation-bound (`RESERVED` at R0 → `ACTIVE` at the cut). Each code is listed in the Configuration group codes table (Taxonomy Standard §3.6), which has no status column, so `ACTIVE` is read from the `cfg_groups.csv` seed row; configuration groups are schema organizers that never identify objects. **The `CFG:` namespace is now complete — all 12 `cfg_groups.csv` rows have a founding record** (1 batch 1 + 11 batch 8). **This closes Category B of the First 100 Records Plan: all 86 dictionary codes across all five namespaces (`SEC:` 29, `ROL:` 17, `ASM:` 16, `FAM:` 12, `CFG:` 12) now have exactly one founding record.** Cohort lifecycle-state counts: 76 `ACTIVE`, 2 `RESERVED`-parked, 5 `SUPERCLASS`, 3 `REJECTED`, 0 `DEPRECATED` (four of five N5 states; `DEPRECATED` structurally absent; 76+2+5+3=86). No status transition undefined in §5; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-12).

- Dictionary founding records, batch 7 (`FAM:` namespace completion): seeded the 11 remaining `FAM:` product family codes under `registry/dictionary/` — `FAM:CLR`/`FAM:DKG`/`FAM:RSP`/`FAM:RPA`/`FAM:SHV`/`FAM:WPL`/`FAM:WKS`/`FAM:MDR`/`FAM:LCK`/`FAM:MBS`/`FAM:STC` — each a verbatim, sourced restatement of its `dictionaries/fam_codes.csv` row (the family CSV carries `domain`/`industry_anchor` in place of a definition; ANSI standard numbers preserved for `CLR`/`DKG`). All are activation-bound (`RESERVED` at R0 → `ACTIVE` at the cut), domain-scoped to `MH`; basis is the Product family codes table (Taxonomy Standard §3.5). The industry anchor is provenance text, not a registered crosswalk. **The `FAM:` namespace is now complete — all 12 `fam_codes.csv` rows have a founding record** (1 batch 1 + 11 batch 7). Category B seeded: 75 of 86 (`SEC:`, `ASM:`, `ROL:`, `FAM:` all complete); N5-state coverage unchanged at four of five. No status transition undefined in §5; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-12).

- Dictionary founding records, batch 6 (`ROL:` namespace completion): seeded the 16 remaining `ROL:` component role codes under `registry/dictionary/` — `ROL:BEM`/`ROL:BRC`/`ROL:ARM`/`ROL:BAS`/`ROL:BPL`/`ROL:DKS`/`ROL:MSH`/`ROL:PAN`/`ROL:SHF`/`ROL:CON`/`ROL:CLP`/`ROL:ADP`/`ROL:GRD`/`ROL:STP`/`ROL:SPC`/`ROL:ANC` — each a verbatim, sourced restatement of its `dictionaries/rol_codes.csv` row (the `ROL:` dictionary carries only `definition`/`successor`). All are activation-bound (`RESERVED` at R0 → `ACTIVE` at the cut); basis is the Component role codes table (Taxonomy Standard §3.3). Component roles are standalone (no `ROL:` superclass) and orthogonal to `SEC:`/`FAM:` per the DAG hierarchy. **The `ROL:` namespace is now complete — all 17 `rol_codes.csv` rows have a founding record** (1 batch 1 + 16 batch 6). Category B seeded: 64 of 86 (`SEC:`, `ASM:`, and `ROL:` all complete); N5-state coverage unchanged at four of five. No status transition undefined in §5; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-12).

- Dictionary founding records, batch 5 (`ASM:` namespace completion): seeded the 14 remaining `ASM:` assembly type codes under `registry/dictionary/` — `ASM:SDK`/`ASM:PDK`/`ASM:BGD` (decking members of the `ASM:DCK` rollup) and the standalone `ASM:FRM`/`ASM:BMA`/`ASM:CBA`/`ASM:ACA`/`ASM:CVB`/`ASM:SHU`/`ASM:WPF`/`ASM:GDA`/`ASM:CNT`/`ASM:DRW`/`ASM:LKR` — each a verbatim, sourced restatement of its `dictionaries/asm_codes.csv` row (the `ASM:` dictionary carries only `definition`/`successor`). All are activation-bound (`RESERVED` at R0 → `ACTIVE` at the cut); basis is the Assembly type codes table (Taxonomy Standard §3.4). **The `ASM:` namespace is now complete — all 16 `asm_codes.csv` rows have a founding record** (1 batch 1 + 1 batch 2 + 14 batch 5). The `ASM:DCK` deck rollup now has all four declared members seeded; `ASM:WDK`'s §5 was aligned to the reciprocal-back-reference convention (additive cross-reference — no status or CSV value changed). Category B seeded: 48 of 86 (`SEC:` and `ASM:` both complete); N5-state coverage unchanged at four of five. No status transition undefined in §5; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-12).

- Dictionary founding records, batch 4 (`SEC:` namespace completion): seeded the 9 standalone `SEC:` shape codes under `registry/dictionary/` — `SEC:IWF`, `SEC:ITF`, `SEC:ANG`, `SEC:TEE`, `SEC:PLT`, `SEC:OMG`, `SEC:SGM`, `SEC:STB`, `SEC:BXB` — each a verbatim, sourced restatement of its `dictionaries/sec_codes.csv` row. All are activation-bound (`RESERVED` at R0 → `ACTIVE` at the cut) and belong to no superclass rollup (each §5 records `Superclass: none`); basis is the §3.2 shape-code table (status `A`). **The `SEC:` namespace is now complete — all 29 `sec_codes.csv` rows have a founding record** (2 batch 1 + 7 batch 2 + 11 batch 3 + 9 batch 4). Every non-`ACTIVE` code in Category B is now on record (the 9 `SEC:` non-active rows + ASM `DCK`), so all remaining Category-B codes are `ACTIVE` in `ROL:`/`ASM:`/`FAM:`/`CFG:`. Category B seeded: 34 of 86; N5-state coverage unchanged at four of five. No status transition undefined in §5; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-12).

- Dictionary founding records, batch 3 (superclass member codes): seeded the 11 `SEC:` member codes under `registry/dictionary/` that the batch-2 superclasses roll up — `SEC:OCU`/`SEC:OCR` (under `SEC:OCS`), `SEC:RHS`/`SEC:SHS`/`SEC:CHS` (under `SEC:HSS`), `SEC:RBR`/`SEC:FBR`/`SEC:HBR` (under `SEC:BAR`), `SEC:ZLP`/`SEC:ZUN` (under `SEC:ZEE`), and the standalone hot-rolled channel `SEC:CHN` — each a verbatim, sourced restatement of its `dictionaries/sec_codes.csv` row. Ten are activation-bound (`RESERVED` at R0 → `ACTIVE` at the cut); `SEC:HBR` is parked-`RESERVED` (the SBR/HBR precedent). Citation basis grounded per code: `OCU`/`OCR`/`CHN` cite founding Rulings R2/R4/R5 (standard Part 3.1); the eight hollow/bar/Z leaves cite the §3.2 shape-code table. Each child names its parent rollup, completing every member list the batch-2 superclass records declared (parent↔child membership consistent); adversarial verification also surfaced that the batch-1 members `SEC:OCL` and `SEC:SBR` lacked the reciprocal §5 superclass back-reference their new siblings carry, so both were updated (additive cross-reference only — no status or CSV value changed), making parent↔child linkage uniform and bidirectional. `SEC:CHN` is recorded standalone, not under the `SEC:OCS` folded rollup (Ruling R5: distinct hot-rolled family). Category B seeded: 25 of 86 (`SEC:` 20 of 29); N5-state coverage unchanged at four of five. No status transition undefined in §5; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-12).

- Dictionary founding records, batch 2 (non-activation-bound status coverage): seeded the 8 non-activation-bound Category-B codes under `registry/dictionary/` — the five `SUPERCLASS` rollups `SEC:OCS`/`SEC:HSS`/`SEC:BAR`/`SEC:ZEE`/`ASM:DCK` and the three `REJECTED` burned strings `SEC:CFS`/`SEC:PIP`/`SEC:TUB` — each a verbatim, sourced restatement of its `dictionaries/*.csv` row. These are **born directly in their assigned state** (matching the CSV `status` at every lineage point), demonstrating the settled semantics: Registry Architecture §8 gates only `RESERVED → ACTIVE`, so non-activation-bound codes never pass through `RESERVED`; `REJECTED` records carry no activation language (a burned string never activates or is reused, N7/RA-2). With batch 2, four of the five N5 states appear in records — `ACTIVE`, `RESERVED`, `SUPERCLASS`, `REJECTED`; `DEPRECATED` has no founding record (it requires a later successor-bearing decision). Category B seeded: 14 of 86. No status transition undefined in §5 introduced; no snapshot cut, no activation, no new record types; no CSV, standard, Explorer, governance, architecture, or Bootstrap-Plan change (2026-07-11).

- Registry status-semantics framing correction: aligned the Batch-1 dictionary records, the six namespace records, and the registry READMEs so `status` consistently means the **assigned registry state at the current lineage point** (Registry Architecture §5), not a temporary pre-snapshot publication flag. Removed the competing "Recorded terminal status" field (folded into one `RESERVED → ACTIVE` trajectory read at two lineage points), distinguished `SEC:SBR`'s parked `RESERVED` from pending-activation `RESERVED`, reframed the `SUPERCLASS`/`REJECTED` deferral as sequencing (born-in-assigned-state) rather than a doctrine blocker, and dropped the unsupported "(N5)" status-authority citation from namespace records. No record `status` values changed; no records created; no architecture/governance/standards/Explorer change. A Bootstrap Plan wording erratum may be proposed separately (2026-07-11).

- Dictionary founding records, batch 1: seeded 6 Category-B dictionary code records under `registry/dictionary/` — `SEC:OCL`, `SEC:SBR`, `ROL:COL`, `ASM:WDK`, `FAM:SPR`, `CFG:DIM` (one code per namespace plus one `RESERVED`-parked SEC code), each a verbatim, sourced restatement of its `dictionaries/*.csv` row. All created in current status `RESERVED` (pre-cut; nothing activates between snapshots, Registry Architecture §8), each documenting the recorded terminal status the `SNAP-1.0.0` cut will confer. `SUPERCLASS`/`REJECTED` codes deferred (their current status is not `RESERVED`). One record per code; no CSV, standard, Explorer, or governance change; no snapshot cut; no activation (2026-07-11).

- Registry artifacts stood up (Registry stage R0): founding record format `docs/FOUNDING_RECORD_TEMPLATE.md` (a minimal instantiation of the Registry Architecture §4.1 public record format — seven blocks, no speculative fields, no performance data); registry directory structure `registry/{namespace,status,dictionary,gsid,component,assembly}/` with per-directory READMEs (owner: Registry Operator only, GOVERNANCE §8; the record-granularity refinement of repository-structure.md's `registry/` export/snapshot sketch); and the six **namespace founding records** (Category A of the First 100 Records Plan) — the `SEC:`, `FAM:`, `ASM:`, `ROL:`, `CFG:` namespaces and the bare Domain namespace — each a verbatim, sourced restatement of its `dictionaries/namespaces.csv` row, `RESERVED` (N5) pending the `SNAP-1.0.0` cut that activates them (nothing activates between snapshots, Registry Architecture §8). Engineering/steward decision trail started at `docs/ENGINEERING_LOG.md`, which carries the authorizing decision the records cite. No records ratified, no snapshot cut, no `[Proposed]` mechanism adopted; no code, dictionary, standard, or Explorer change (2026-07-11).

- Erratum in `docs/FIRST_100_RECORDS_PLAN.md`: corrected the founding-cohort activation tally to count the ASM superclass `DCK` — 5 `SUPERCLASS` rows not 4, ≈98 records activate at the cut not ≈99, 10 non-`ACTIVE` dictionary rows not 9. The 108-record cohort total is unchanged (`DCK`'s record was always counted; only its status was mis-tallied). Surfaced by adversarial verification of the namespace founding records (2026-07-11).

- First-100-records plan added: `docs/FIRST_100_RECORDS_PLAN.md` — exact founding cohort of `SNAP-1.0.0` (6 namespace + 86 dictionary + 4 template + 8 flagship-geometry + 3 component + 1 assembly = 108 registered records, ~85% ratified dictionary); enumerates records already implicit in standards/samples/scenarios, the illustrative-serial reconciliation, zero-permission vs governance-gated vs deferred (AISC designations, non-WDK products, evidence, aliases) records, creation sequence, snapshot cut line, and the SNAP-1.0.0 readiness checklist; creates no records (2026-07-11).

- Bootstrap plan added: `docs/SECTIONHUB_BOOTSTRAP_PLAN.md` — practical execution from architecture to useful registry without prior industry adoption (first ~110 records incl. seed-dictionary ratification and worked-example GSIDs; first 500 via legacy-profile, cleared-designation, and decking tranches; avoided sources; curation-vs-import split; SMHE asset leverage; taxonomy-explosion defenses; minimum-usefulness thresholds; S2/S3/S4/A6 readiness checklists; Q1–Q4 roadmap gated on canonicalization, templates, LICENSE, and demo-alias resolution); no code, APIs, schemas, or UI work (2026-07-11).

- Data submission model added: `docs/SECTIONHUB_DATA_SUBMISSION_MODEL.md` — constitutional model for how information enters the future registry (seven submission roles, twelve submission object types, [Proposed] seven-label provenance scheme superseding the two-grade naming on adoption, evidence minimums with data-minimization, public/controlled boundary, review paths per the TC–Operator procedure test, eight refusal rules, correction-by-supersession, [Proposed] dispute mechanics, certification boundary, Explorer-as-view, anti-patterns DS-1..DS-11, staged program S0–S5 with legal/licensing gates); no code, forms, APIs, or Explorer changes (2026-07-11).

- Registry constitutional design added: `docs/SECTIONHUB_REGISTRY_ARCHITECTURE.md` — 30-year registry architecture (record object model, append-only immutability doctrine, proposed general lifecycle reconciled with N5 statuses, public record format, crosswalk/alias/snapshot models, TC–Operator authority boundary, registry anti-patterns RA-1..RA-14, success criteria, R0–R4 roadmap); new mechanisms marked [Proposed] pending adoption; no software/API/database design (2026-07-11).

- Code Dictionary Standard: linked the operational proposal workflow and issue templates from its proposal-rules clause (editorial bridge; completes the standards-split cross-referencing — the split itself landed in commit ee28c98) (2026-07-11).

- Public-readiness audit (`docs/PUBLIC_READINESS_AUDIT.md`): README rewritten for first-time visitors (live Explorer link up top, no-performance-claims disclaimer, status-at-a-glance table, grouped contents); LICENSE placeholder notice added (no license granted yet); dead empty `dictionaries/` PascalCase CSVs and stray `app/` directory removed per repository-structure migration step 2; corrupted `docs/governance-note-demo-code-reconciliation.md` repaired (PowerShell wrapper → intended content + pointer to the authoritative DEFERRED record); live Explorer URL added to explorer/README. No Explorer behavior, dictionary content, or demo-alias decisions changed (2026-07-11).

- Demo scenario pack added: `docs/demo-scenarios.md` plus `examples/scenarios/` — six audience walkthroughs (manufacturer section, lab wire deck, SKU mapping, inspector W-201 refusal, code proposal, future certification), each tagged EXPLORER_REPRODUCIBLE or ILLUSTRATIVE_DATA_MODEL; no dictionary or Explorer changes (2026-07-11).
- Public position paper added: `docs/position-paper-why-sectionhub.md` — the deterministic-identity gap relative to AISC/AISI/RMI/SMA/GS1/UNSPSC/ECLASS/ETIM/OmniClass/MasterFormat/SKUs, with explicit non-affiliation and non-substitution posture (2026-07-11).
- Formal proposal workflow established: `docs/proposal-workflow.md` (lifecycle, evidence gates, decision categories, outcomes, propagation) plus issue templates `proposals/TEMPLATE-code-change.md`, `proposals/TEMPLATE-taxonomy-change.md`, `proposals/TEMPLATE-crosswalk-change.md`; README "How to propose changes" section; GOVERNANCE.md and CONTRIBUTING.md pointers (2026-07-11).
- Standards split (working copies): populated `standards/GSID_2D_Standard.md`, `standards/Material_Handling_Taxonomy_Standard.md`, and `standards/Code_Dictionary_Standard.md` by verbatim extraction from the unified v0.1 draft, which remains the canonical document of record; original clause numbering and rule IDs retained; editorial bridge text marked; no normative meaning changed (2026-07-11).
- SECTIONHUB_STRATEGIC_ARCHITECTURE.md added: 10-year ecosystem strategy (governance eras G1–G3, adoption stages A1–A7, external-standards relationships, XMAP crosswalk architecture, certification model, failure modes F-1..F-16) (2026-07-11).
- ROADMAP.md repaired: restored the seven-phase roadmap from commit e8c9c5b (the file had been overwritten with an unexecuted PowerShell fragment in commit d996b85, breaking GOVERNANCE.md's "Phase 7" bootstrap anchor) (2026-07-11).

- Explorer Phase 2 — Rule Traceability & Standards Credibility Layer: machine-readable rule table `explorer/rules/question_nodes.csv`; Decision trace in UI and JSON (`decision_trace`); first-class W-201 refusal panel (why stopped / what's needed / why no guessing); dictionary provenance display with status badges and demo-code banner; refusal sample `explorer/samples/section-indeterminate-w201.json`; demo-alias governance note `proposals/2026-07-03-phase1-demo-alias-deviations.md` (2026-07-03).
- Explorer Phase 1 MVP under `explorer/`: static deterministic wizard for Section/Component/Assembly with demo dictionary subset `SNAP-0.1.0-DEMO` (2026-07-02).
- Governance documents established: CONTRIBUTING.md, GOVERNANCE.md, CODE_OF_CONDUCT.md, REJECTIONS.md, proposals/, CHANGELOG.md (2026-07-02).

## [SNAP-1.0.0] — draft seed, 2026-07-02 (not yet released)

- Seed dictionaries published under `dictionaries/`: `namespaces.csv`, `sec_codes.csv`, `rol_codes.csv`, `asm_codes.csv`, `fam_codes.csv`, `cfg_groups.csv`, matching Part 3 of the Taxonomy Standard draft.
- Founding rulings R1–R6 (OCL/OCU/OCR adopted; OCS demoted to SUPERCLASS; CHN adopted; CFS/PIP/TUB rejected) — see the standard Part 3.1 and REJECTIONS.md.
